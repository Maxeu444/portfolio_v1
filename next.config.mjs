/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
  // Standalone build for VPS deployment
  output: 'standalone',
};

export default nextConfig;
